{
  "name": "backend",
  "version": "0.0.1",
  "private": true,
  "description": "STORY 2.3 experimental Backstage harness backend -- see docs/backstage-compatibility.md. Intentionally minimal: guest auth (so the httpAuth-gated servicelog route is reachable at all, see plugin #9) plus the servicelog backend plugin. No catalog/search/techdocs/scaffolder.",
  "main": "dist/index.cjs.js",
  "types": "src/index.ts",
  "backstage": {
    "role": "backend"
  },
  "scripts": {
    "start": "backstage-cli package start",
    "build": "backstage-cli package build",
    "lint": "backstage-cli package lint",
    "test": "backstage-cli package test",
    "clean": "backstage-cli package clean"
  },
  "dependencies": {
    "@backstage/backend-defaults": "^0.17.7",
    "@backstage/plugin-auth-backend": "^0.30.0",
    "@backstage/plugin-auth-backend-module-guest-provider": "^0.2.22",
    "@internal/plugin-servicelog-backend": "0.0.1"
  },
  "devDependencies": {
    "@backstage/cli": "^0.36.5"
  },
  "files": [
    "dist"
  ]
}
